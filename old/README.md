# Archived — Animei (old)

Archived copies of previous bot versions. Kept for reference only; not actively maintained.

Quick start
- Activate your virtual environment.
- Install dependencies: `pip install -r requirements.txt`.
- Set `DISCORD_BOT_TOKEN` in your environment.
- Run an archived script (example): `python bot_simple.py`.
